{
  "name": "cashpro",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicación móvil de gestión financiera personal",
  "scripts": {
    "dev": "pnpm -C apps/mobile start",
    "dev:mobile": "pnpm -C apps/mobile start",
    "doctor:mobile": "pnpm -C apps/mobile dlx expo-doctor",
    "prebuild:mobile": "pnpm -C apps/mobile dlx expo prebuild --clean",
    "build:android:preview": "pnpm -C apps/mobile dlx eas-cli build --platform android --profile preview",
    "build:android:preview:local": "pnpm -C apps/mobile dlx eas-cli build --platform android --profile preview --local"
  },
  "packageManager": "pnpm@10.30.0",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
